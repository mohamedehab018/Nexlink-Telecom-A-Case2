"""Nexlink planning evaluation harness (Person 2 scope).

Mirrors the `planning_eval/` folder required by the project brief: a test-case
suite (`test_cases.py`), the real incident bundles (`scenarios.py`), the
runner that compares every planning method and saves `artifacts/run-*.json`
traces, and the tests proving the harness itself works.
"""
